set(vtktiff_LIBRARIES vtktiff)
vtk_module_impl()
set(VTK_ZLIB_LIBRARIES ${vtkzlib_LIBRARIES})
set(VTK_JPEG_LIBRARIES ${vtkjpeg_LIBRARIES})
configure_file(vtk_tiff.h.in vtk_tiff.h)
install(FILES ${CMAKE_CURRENT_BINARY_DIR}/vtk_tiff.h DESTINATION ${VTK_INSTALL_INCLUDE_DIR})
add_subdirectory(vtktiff)
